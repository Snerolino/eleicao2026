---
description: Send a task to Jules (Google's AI coding agent)
---

Delegate the following task to Jules, Google's asynchronous coding agent.

Task: $ARGUMENTS

Run `jules remote new --repo . "$ARGUMENTS"` and report back the session status.
