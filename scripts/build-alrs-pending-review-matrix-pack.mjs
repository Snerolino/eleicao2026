#!/usr/bin/env node
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root=resolve(import.meta.dirname,'..');const input=resolve(root,'data/legislative-import/alrs/editorial-approval-pack-v1.json');const output=resolve(root,'data/legislative-import/alrs/impact-matrix-pending-review-pack-v1.json');const q=JSON.parse(readFileSync(input,'utf8'));const items=(q.items??[]).filter(x=>x.editorial_disposition==='assess').map(x=>({proposition_version_id:x.proposition_version_id,review_key:x.review_key,official_match_key:x.official_match_key,title:x.title,methodology_version:'impacto-populacional-v1',review_status:'pending_review',editorial_status:'approved',substantive_source_gate:x.substantive_source_gate,source_durability_gate:x.source_durability_gate,assessments:x.assessments,external_review_required:(x.assessments??[]).some(a=>a.requires_external_review),human_review_required:true,public_approval:false,remote_apply:false}));const result={schema_version:'1.0.0',packet_type:'alrs_impact_matrix_pending_review_pack',review_status:'pending_review',public_approval:false,remote_apply:false,apply_ready:false,totals:{matrices:items.length,assessment_rows:items.reduce((s,x)=>s+x.assessments.length,0),external_review_required:items.filter(x=>x.external_review_required).length},items};writeFileSync(output,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({output,...result.totals}));
