#!/usr/bin/env node
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root=resolve(import.meta.dirname,'..');const q=JSON.parse(readFileSync(resolve(root,'data/legislative-import/alrs/impact-review-queue-v1.json'),'utf8'));const items=(q.items??[]).filter(x=>x.priority==='P2'&&x.event_type==='merit_candidate'&&!x.version_key_collision&&x.title_quality!=='generic').slice(10,15).map(x=>({proposition_version_id:x.proposition_version_id,review_key:x.review_key,title:x.title,priority:x.priority,event_type:x.event_type,source_urls:x.source_urls??[],candidate_source_links:x.candidate_source_links??[],source_status:'needs_substantive_review',editorial_disposition:'pending_review',human_review_required:true,remote_apply:false}));const result={schema_version:'1.0.0',packet_type:'alrs_p2_microbatch_3',remote_apply:false,public_approval:false,totals:{versions:items.length},items};writeFileSync(resolve(root,'data/legislative-import/alrs/p2-microbatch-3-2026-08-23.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({versions:items.length,items:items.map(x=>x.proposition_version_id)}));
