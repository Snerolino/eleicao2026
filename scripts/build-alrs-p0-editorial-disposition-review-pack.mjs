#!/usr/bin/env node
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root=resolve(import.meta.dirname,'..');const input=resolve(root,'data/legislative-import/alrs/confirmed-merit-review-pack-v1.json');const output=resolve(root,'data/legislative-import/alrs/p0-editorial-disposition-review-pack-v1.json');const q=JSON.parse(readFileSync(input,'utf8'));const items=(q.items??[]).filter(x=>x.priority==='P0').map(x=>({proposition_version_id:x.proposition_version_id,review_key:x.review_key,title:x.title,official_merit_confirmed:x.official_merit_confirmed,substantive_source_gate:x.substantive_source_gate,source_durability_gate:x.source_durability_gate,editorial_disposition:'pending_review',disposition_rationale:null,assessments:[],human_review_required:true,remote_apply:false}));const result={schema_version:'1.0.0',packet_type:'alrs_p0_editorial_disposition_review_pack',review_status:'pending_review',remote_apply:false,public_approval:false,totals:{versions:items.length,dispositions_pending:items.filter(x=>x.editorial_disposition==='pending_review').length},items};writeFileSync(output,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({output,...result.totals}));
