import fs from 'node:fs';
function arg(name){const i=process.argv.indexOf(name);return i>=0?process.argv[i+1]:null;}
const input=arg('--redteam'),out=arg('--out');if(!input||!out)throw new Error('use --redteam e --out');const d=JSON.parse(fs.readFileSync(input,'utf8'));const rows=Array.isArray(d)?d:(d.projects??d.items??[]);const items=rows.map(x=>({id:x.id??x.project_id,decision:'withheld',score_eligible:false,reason:x.reason??x.risco??'red-team retenção fail-closed',risk:x.risk??x.risco??'high',missing_sources:x.missing_sources??x.fontes_faltantes??[],counterargument:x.counterargument??x.contra_argumento??null,identity:x.identity??null,authorship:x.authorship??null,full_text:x.full_text??null,event_version:x.event_version??null,reconciliation:'Red-team independente confirmou retenção; autoria não equivale a voto e não há vínculo completo versão/evento.'}));if(items.length!==25)throw new Error(`expected 25, got ${items.length}`);fs.writeFileSync(out,JSON.stringify({schema_version:'1.0.0',packet_type:'candidate_authored_projects_reconciled_editorial_review',mode:'withheld',remote_apply:false,content_read:true,source:'official_full_text',counts:{items:25,approved:0,pending_review:0,withheld:25,score_eligible:0},items},null,2)+'\n');console.log(JSON.stringify({items:25,approved:0,withheld:25,score_eligible:0,output:out}));
