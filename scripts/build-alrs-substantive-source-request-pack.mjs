#!/usr/bin/env node
import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
const root=resolve(import.meta.dirname,'..');const input=resolve(root,'data/legislative-import/alrs/confirmed-merit-assessment-proposal-pack-v1.json');const output=resolve(root,'data/legislative-import/alrs/substantive-source-request-pack-v1.json');const q=JSON.parse(readFileSync(input,'utf8'));const items=(q.items??[]).flatMap(item=>(item.proposed_assessments??[]).map(assessment=>({proposition_version_id:item.proposition_version_id,review_key:item.review_key,title:item.title,group_slug:assessment.group_slug,official_vote_sources:item.official_sources??[],required_source_types:['texto_integral_versao','parecer_ou_substitutivo','resultado_oficial_ou_tramitacao'],source_request_status:'pending_substantive_source',human_review_required:true,remote_apply:false})));const result={schema_version:'1.0.0',packet_type:'alrs_substantive_source_request_pack',remote_apply:false,public_approval:false,totals:{requests:items.length,versions:new Set(items.map(x=>x.proposition_version_id)).size},items};writeFileSync(output,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({output,...result.totals}));
